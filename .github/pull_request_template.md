## 关联 Issue

<!-- 可选。使用 `Closes #123` 关闭 Issue，使用 `Refs #123` 仅关联；没有关联 Issue 时填写“无”。 -->

## 变更说明

<!-- 简述本 PR 的目标、修改内容及原因。 -->

## 文件修改

<!--
一句话，不超过一行；改了什么不复述（见 Files changed）。
背景或权衡较长时，写到「变更说明」，此处只留结论。
同一原因涉及多个文件时合并为一行，或用通配符。
-->

| 文件路径 | 原因（一句话） |
| --- | --- |
| `path/to/file` | 为什么动它 |

## 验证

<!-- 列出实际执行的命令及结果；未执行时说明原因。 -->

- `command`：结果

## 兼容性与风险

<!--
仅说明以下情况；均不存在时填写“无”：
- Fabric 与 NeoForge 行为不一致
- 存档、持久化数据或配置格式变化
- 性能或资源占用变化
- 玩家可见的破坏性行为变化
- 偏离现有 CONTEXT.md 或 ADR
-->
